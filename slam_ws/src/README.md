# slam_ws/src

Clone upstream SLAM and planning repositories into this directory.

This workspace is for upstream packages that should remain as close to upstream as possible. Keep local code out of this directory unless you are intentionally evaluating a fork.

Use `manifests/slam_ws.repos` as the source of truth for repository URLs and pinned revisions.
